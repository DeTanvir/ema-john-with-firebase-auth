/**
 * to install and use firebase
 * 1. create a firebase project from firebase console
 * 2. create a web firebase app with the project
 * 3. npm i firebase
 * 4. save firebase config and export default app
 * 5. Build > Authentication > get started > enable signin method
 * 6. create login and sign up route
 */

/**
 * context api
 * 1. create a context provider component
 * 2. create a context and set provider
 * 3. set the children props
 * 4. set context value
 * 5. set the provider
 */


/**
 * FIREBASE HOSTING
 * Once per pc
 * 1. npm install -g firebase tools
 * 2. firebase login
 * 
 * Once for each project
 * 1. firebase init
 * 2. proceed
 * 3. hosting: firebase(up and down arrow) and use space bar to select
 * 4. existing project
 * 5. select the project carefully
 * 6. which project as public directory: dist
 * 7. single page application: yes
 * 8. continuous deployment at github: no
 */

/**
 * For Each Deploy
 * 1. npm run build
 * 2. firebase deploy
 */